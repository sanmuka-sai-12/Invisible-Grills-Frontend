---
import logoImg from '../../../assets/Images/HomePageImg/FirstHomeFrame/logo.png?url';
---

<header class="header">
    <div class="header-content">
        <div class="logo-wrapper">
            <img src={logoImg} alt="DS Invisible Grills Logo" />
        </div>
        <nav class="nav">
            <button class="nav-btn" id="home-btn">Home</button>
            <button class="nav-btn" id="about-btn">About Us</button>
            <button class="nav-btn dropdown" id="solutions-btn">
                Solutions 
                <svg width="12" height="8" viewBox="0 0 12 8" fill="none">
                    <path d="M1 1.5L6 6.5L11 1.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
            </button>
            <button class="nav-btn">Gallery</button>
            <button class="nav-btn" id="contact-btn">Contact Us</button>
        </nav>
        <button class="enquiry-btn">
            <span>Enquiry Now</span>
            <svg class="icon" width="16" height="16" viewBox="0 0 24 24" fill="none">
                <path d="M6.62 10.79a15.05 15.05 0 006.59 6.59l2.2-2.2a1 1 0 011.01-.24c1.12.37 2.33.57 3.58.57a1 1 0 011 1V20a1 1 0 01-1 1C10.3 21 3 13.7 3 4a1 1 0 011-1h3.5a1 1 0 011 1c0 1.25.2 2.46.57 3.59a1 1 0 01-.25 1.01l-2.2 2.19z" fill="white" />
            </svg>
        </button>
    </div>
</header>

<style>
    .header {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        width: 100%;
        height: 96px;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 0;
        z-index: 10;
        background: #FAFAFA;
    }

    .header-content {
        position: relative;
        width: 1440px;
        height: 96px;
        display: flex;
        align-items: center;
        padding: 0 120px;
    }

    .logo-wrapper {
        width: 160px;
        height: 100px;
        display: flex;
        align-items: center;
        margin-right: 220px;
    }

    .logo-wrapper img {
        width: 100%;
        height: 100%;
        object-fit: contain;
        display: block;
    }

    .nav {
        display: flex;
        gap: 50px;
        margin-right: 220px;
    }

    .nav-btn {
        background: transparent;
        border: none;
        outline: none;
        font-family: 'Inter', sans-serif;
        font-size: 20px;
        font-weight: 500;
        color: #000000;
        padding: 0;
        cursor: pointer;
        position: relative;
        display: flex;
        align-items: center;
        gap: 4px;
    }

    .nav-btn.dropdown svg {
        margin-left: 4px;
    }

    .nav-btn:hover,
    .nav-btn:focus,
    .nav-btn:active {
        background: transparent;
        outline: none;
        box-shadow: none;
    }

    .enquiry-btn {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        padding: 12px 24px;
        height: 45px;
        background: #B37A42;
        border: none;
        border-radius: 54px;
        cursor: pointer;
    }

    .enquiry-btn span {
        font-family: 'Inter', sans-serif;
        font-size: 16px;
        font-weight: 600;
        line-height: 132%;
        letter-spacing: 0;
        color: #ffffff;
    }

    .enquiry-btn .icon {
        display: block;
    }
</style>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        const homeBtn = document.getElementById('home-btn');
        const aboutBtn = document.getElementById('about-btn');
        const solutionsBtn = document.getElementById('solutions-btn');
        const contactBtn = document.getElementById('contact-btn');
        
        if (homeBtn) {
            homeBtn.addEventListener('click', function() {
                window.location.href = '/';
            });
        }
        
        if (aboutBtn) {
            aboutBtn.addEventListener('click', function() {
                window.location.href = '/about';
            });
        }
        
        if (solutionsBtn) {
            solutionsBtn.addEventListener('click', function() {
                window.location.href = '/Solution';
            });
        }
        
        if (contactBtn) {
            contactBtn.addEventListener('click', function() {
                window.location.href = '/ContactUs';
            });
        }
    });
</script>


